{
  "name": "pocketstream",
  "private": true,
  "version": "0.1.0",
  "description": "A low-latency Unity cloud gaming prototype for mobile browsers.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace web-client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web-client"
  },
  "workspaces": [
    "server",
    "web-client"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
